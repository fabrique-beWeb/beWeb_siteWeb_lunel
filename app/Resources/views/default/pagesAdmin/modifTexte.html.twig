{% extends 'base.html.twig' %} 
{% block body %}

    <div class="container">
        <div class="col-lg-offset-2 col-lg-8 col-lg-offset-2">
            {{form_start(formtexte,{'attr':{'action':path('updateSection',{'id':id})}})}} {#Methde pour valide #}
            <div class="input-group" >
                <span class="input-group-addon" >Titre</span>

                {{ form_widget(formtexte.titre, { 'attr': {'class': 'form-control'} })}}
            </div>
            <div class="input-group">
                <span class="input-group-addon" >Texte</span>
                {{ form_widget(formtexte.texte, { 'attr': {'class': 'form-control'} })}}
            </div>
            {{form_end(formtexte)}}
        </div>
    </div>

{% endblock %}